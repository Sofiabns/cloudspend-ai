# Public demo mode. Set to aws only in a private, trusted environment.
APP_MODE=demo
AWS_REGION=us-east-1
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# Standard AWS credential-chain variables may be configured locally.
# Never commit AWS_ACCESS_KEY_ID or AWS_SECRET_ACCESS_KEY.
